const EVENT_TYPES = {
    WebPageView: 'WebPageView'
}

const SUB_SYSTEMS = {
    CambridgeOne: 'c1'
}

const URL_ENDPOINTS = {
    ServerAnalytics: '/apigateway/cav-analytics'
}

const APP_METADATA = {
    AppVersion: 'v2'
}


module.exports = {
    EVENT_TYPES,
    SUB_SYSTEMS,
    URL_ENDPOINTS,
    APP_METADATA
}